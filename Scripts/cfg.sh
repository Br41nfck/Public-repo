#!bin/bash
# Autoinstall my programs setup
# use # for comment not needed programs
# OS - Debian 10 Buster

# Update and install all installed programs
sudo apt update && sudo apt upgrade

# Git
sudo apt install git

# Emacs
sudo apt install emacs

# VLC Media Player
sudo apt install vlc

# QuteBrowser
sudo apt install qutebrowser

# Discord
sudo apt install discord

# Libre Office
sudo apt install libreoffice

# Telegram
sudo apt install telegram

# Filezilla
sudo apt install filezilla

# Deluge
sudo apt install deluge

# GParted
sudo apt install gparted

# TimeShift
sudo apt install timeshift

# Thunderbird
sudo apt install thunderbird

# Books
# Calibre - electronic library
sudo apt install calibre
# DjView - reader for .djview files
sudo apt install djview
# Okular - reader for .pdf files
sudo apt install Okular
# YACReader - (build from sources) reader for .cbr files (comics)  
